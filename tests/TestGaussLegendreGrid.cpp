#include <gtest/gtest.h>

/*

#include "CheckCoeff2Coeff.h"

TEST(GaussLegendreGrid, Coeff2CoeffFloatR2C) {
  bool result = Coeff2Coeff<float, NonNegative, NonNegative, float>();
  EXPECT_TRUE(result);
}



TEST(GaussLegendreGrid, Coeff2CoeffDoubleR2C) {
  bool result = Coeff2Coeff<double, R2C>();
  EXPECT_TRUE(result);
}

TEST(GaussLegendreGrid, Coeff2CoeffLongDoubleR2C) {
  bool result = Coeff2Coeff<long double, R2C>();
  EXPECT_TRUE(result);
}

TEST(GaussLegendreGrid, Coeff2CoeffFloatC2C) {
  bool result = Coeff2Coeff<float, C2C>();
  EXPECT_TRUE(result);
}

TEST(GaussLegendreGrid, Coeff2CoeffDoubleC2C) {
  bool result = Coeff2Coeff<double, C2C>();
  EXPECT_TRUE(result);
}

TEST(GaussLegendreGrid, Coeff2CoeffLongDoubleC2C) {
  bool result = Coeff2Coeff<long double, C2C>();
  EXPECT_TRUE(result);
}

*/
