#ifndef GSH_TRANS_FIELD_MODULE_H
#define GSH_TRANS_FIELD_MODULE_H

// Spin fields: a field of definite upper index on the sphere, and its lazy
// algebra. This replaces the CanonicalComponentField tree, which stored
// operands as references to a CRTP base (so nested expressions dangled), did
// not reverse the upper index under conjugation, and offered real and imag at
// every upper index where they are covariant only at zero.
#include "src/SpinField/SpinField.h"
#include "src/SpinField/SpinFieldNodes.h"
#include "src/SpinField/SpinFieldOverloads.h"
#include "src/SpinField/SpinFieldView.h"
#include "src/SpinField/SpinWeighted.h"

#endif
